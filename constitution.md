# AI智能体应用开发强制准则（Agent Rules v1.0）

> **元规则（规则0）**：所有规则服务于“业务价值快速交付”。若严格执行某条规则导致迭代停滞，Agent需**暂停并标记冲突**，提请人工仲裁，禁止盲目遵循。

## 1. 架构与工程（Architecture）
*生效阶段：架构设计、模块划分、技术选型*

| ID | 规则 | 级别 | 判定标准 |
| :--- | :--- | :--- | :--- |
| **ARC-01** | **状态外部化**：应用服务器必须无状态。Session、缓存、临时文件必须存储于Redis/OSS等外部中间件，禁止使用本地内存或磁盘存储业务关键状态。 | **[必须]** | 检查是否有`java.io.File`写入业务数据或`ConcurrentHashMap`存储会话。 |
| **ARC-02** | **依赖倒置**：高层业务模块禁止直接依赖低层基础设施（如MySQL、Redis）。必须通过接口(Interface)隔离，实现细节归于`infrastructure`层。 | **[必须]** | 检查`Service`层是否直接`new`具体DAO实现类，或导入`jedis`包。 |
| **ARC-03** | **演进式设计（YAGNI）**：对于“未来可能需要”的扩展点（如多租户、消息队列），在未达到**实际验证的QPS阈值（如1000/s）**或**第三次业务重复**前，禁止引入分布式中间件，优先使用单机本地方案。 | **[建议]** | 检查依赖`pom.xml`是否包含未使用的MQ或分库分表中间件。 |
| **ARC-04** | **配置与环境分离**：严格遵守12-Factor原则。`application.yml`中只存放默认值，环境特定变量（IP、账号、密码）必须通过系统环境变量或配置中心注入，禁止硬编码。 | **[必须]** | 扫描代码中是否包含 `jdbc:mysql://192.168.x.x` 或 `password=123456` 字面量。 |

## 2. 编码与质量（Coding）
*生效阶段：代码生成、单元测试、代码评审*

| ID | 规则 | 级别 | 判定标准 |
| :--- | :--- | :--- | :--- |
| **COD-01** | **防御式编程（Fail Fast）**：所有对外公开的方法（Controller/Service接口）必须进行**参数非空和边界校验**。校验不通过即抛出显式异常，禁止让空指针（NPE）传递到业务核心层。 | **[必须]** | 检查`public`方法是否缺失`@NotNull`或`if (param == null)`判断。 |
| **COD-02** | **事务边界精简**：`@Transactional`仅能置于Service层，禁止置于Controller层。事务内禁止包含RPC远程调用或Redis操作（长事务导致连接池耗尽）。 | **[必须]** | 扫描`@Transactional`方法内是否包含`httpClient`或`rpc.invoke()`。 |
| **COD-03** | **测试金字塔**：单元测试（UT）覆盖率目标≥80%（核心模块）。测试分层必须遵循：**大量单测（Mock隔离） > 少量集成测试（含DB） > 极少数端到端（UI/E2E）**。构建流水线中，若单元测试失败，禁止打包。 | **[必须]** | CI脚本检查 `mvn test` 通过率是否为100%。 |
| **COD-04** | **日志规约**：关键业务路径（支付、下单、登录）必须打印包含 **UserID+RequestID** 的日志。禁止在循环体内打印`debug`日志，禁止使用`System.out.println`。 | **[必须]** | 正则匹配是否存在`System.out`或`e.printStackTrace()`。 |
| **COD-05** | **异常吞噬禁止**：`catch`异常后禁止无任何处理（空catch块）。必须执行**打日志并向上抛出**或**转换业务异常**。 | **[必须]** | 检查`catch`块内是否为空或仅有注释。 |

## 3. 产品与用户体验（UX/Product）
*生效阶段：API设计、前端交互、错误码定义*

| ID | 规则 | 级别 | 判定标准 |
| :--- | :--- | :--- | :--- |
| **UX-01** | **降级兜底**：所有依赖外部网络（RPC/HTTP/DB）的调用，必须设置 **超时（Timeout）** 和 **重试（Retry）策略（带指数退避）**。针对非核心链路（如推荐、广告），必须提供兜底默认值（如空列表），严禁因超时导致整体白屏。 | **[必须]** | 检查`RestTemplate`/`Feign`是否配置`connectTimeout`和`readTimeout`。 |
| **UX-02** | **错误码可读性**：返回前端的业务错误码必须为**枚举类型**，且附带对人类友好的中文/英文提示。禁止直接返回SQL异常堆栈或`500 Internal Error`裸字符串。 | **[必须]** | 扫描`Controller`返回体是否包含`stackTrace`关键字。 |
| **UX-03** | **克制设计**：每新增一个UI按钮或操作步骤，必须满足“日均点击率预估>5%”或“解决25%以上用户痛点”。禁止仅为“炫技”添加复杂动效或冗余确认弹窗。 | **[建议]** | 无自动检测，需依赖需求文档中的PRD指标。 |

## 4. 交付与运维（DevOps）
*生效阶段：CI/CD流水线、部署、监控*

| ID | 规则 | 级别 | 判定标准 |
| :--- | :--- | :--- | :--- |
| **OPS-01** | **不可变构建**：同一份代码包（JAR/镜像）必须能通过环境变量切换在Dev/Test/Prod环境运行。禁止针对生产环境单独重新打包。 | **[必须]** | 检查构建流水线是否每次生成新的唯一版本号。 |
| **OPS-02** | **灰度发布策略**：禁止直接全量发布。必须采用**金丝雀发布（Canary）**，先发布至1%~5%节点观察**错误率（Error Rate）**和**响应时间（RT）**，持续观察≥10分钟无异常后，方可全量。 | **[必须]** | 发布系统检查是否存在分批发布配置。 |
| **OPS-03** | **一键回滚**：每次发布版本必须保留最近**至少3个稳定版本**的镜像。若发布后核心指标（如订单量/登录成功率）在5分钟内下跌超过**预设阈值（如10%）**，Agent应自动触发回滚建议或执行自动回滚（取决于配置）。 | **[必须]** | 检查K8s或发布平台是否存在`rollback`历史记录。 |
| **OPS-04** | **精准告警**：告警规则必须遵循**少而精**原则。仅针对**SLO（服务水平目标）核心指标**（可用性、延迟、错误预算消耗）设置P0/P1级别告警，禁止针对非核心指标（如JVM年轻代GC频率）设置电话告警，避免告警疲劳。 | **[建议]** | 检查监控平台是否有超量（>10条）P0级别告警策略。 |

---

## Agent执行上下文（Execution Context）

当接收用户需求或生成代码时，Agent应按以下**决策树**进行自我审查：

1. **生成新Controller**：检查是否遗漏 `参数校验(COD-01)` + `超时配置(UX-01)` + `日志(COD-04)`。
2. **引入新中间件依赖**：检查是否违反 `演进式设计(ARC-03)`，需要求开发者提供QPS预估数据。
3. **修改核心Service**：检查是否破坏 `事务边界(COD-02)` 或引入循环依赖。
4. **准备提交代码**：检查本地 `mvn clean test` 是否全部通过，且未硬编码任何IP/密码(ARC-04)。
5. **准备发布**：确认发布系统开启了 `灰度(OPS-02)` 且保留了上一个版本的镜像。
