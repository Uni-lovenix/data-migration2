# 目标
1. 支持从mysql、sqlite、access、neo4j、hive中导出数据
2. 支持将导出的数据，导入到elasticsearch、postgresql、hive、mysql数据库中
3. 支持指定字段导入
4. 每一步都设计成原子化操作，可以复用。通过编排这些操作，就可以成为一个连续的作业
5. 所有的工具调用，导入导出操作。都支持agent调用。用于后面做成一个agentic工具的操作储备。
6. 核心功能：支持不同数据在不同的数据库中进行流动，对于数据类型不兼容的数据，默认转换json格式。也支持用户指定转换类型。


## 开发原则
1. DRY原则
2. SSOT原则
3. 原则是地图，不是法律。先理解问题，再选原则，最后看成本和可逆性。